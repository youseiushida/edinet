## H-6. XBRL のバージョン

### 質問への対応

| サブ質問 | 回答 |
|----------|------|
| H-6.1 XBRL 仕様のバージョン | XBRL 2.1 |
| H-6.2 XBRL Dimensions 仕様のバージョン | XBRL Dimensions 1.0 |
| H-6.3 OIM / xBRL-JSON / xBRL-CSV への移行予定 | 仕様書に一切言及なし。移行予定は確認できない |

### 情報源（Fact）

- [F1] パス: `docs/仕様書/EDINETフレームワーク設計書.md` L.186-201 — 「EDINET タクソノミで採用する XBRL の技術仕様及びベストプラクティス」として以下の 7 項目が列挙:
  1. XBRL 2.1 Specification — タクソノミ
  2. XBRL Dimensions 1.0 — ディメンション
  3. Generic Labels 1.0 — 拡張リンクロールの英語ラベル
  4. Inline XBRL 1.0 — 提出 XBRL 書類
  5. FRTA — タクソノミ
  6. FRIS — インスタンス
  7. GFM — 提出 XBRL 書類
- [F2] 検索結果: `docs/仕様書/` 配下の全ファイルに対し「OIM」「xBRL-JSON」「xBRL-CSV」「Open Information Model」を検索 — **該当なし**（0 件）
- [F3] 検索結果: `docs/仕様書/` 配下の全ファイルに対し「XBRL 2.2」を検索 — **該当なし**（0 件）

### 推論（Reasoning）

1. [F1] より、EDINET は XBRL 2.1 Specification を採用している。XBRL 2.2 は [F3] のとおり言及がなく、採用されていない。
2. [F1] より、Dimensions は 1.0 を採用している。
3. [F1] の 7 項目に OIM / xBRL-JSON / xBRL-CSV は含まれず、[F2] で仕様書全体を検索しても該当がないため、現時点で移行予定は確認できない。
4. パーサー実装への影響: XBRL 2.1 + Dimensions 1.0 + Inline XBRL 1.0 のみを対象とすればよく、OIM 系フォーマットへの対応は不要。

### 確信度

- 高（仕様書に明記 + 否定検索で移行予定なしを確認）
