## A-6. Tuple 型 Fact

### 質問への対応

| サブ質問 | 回答 |
|----------|------|
| A-6.1 `xbrli:tuple` は使われるか | NO。EDINET の XBRL インスタンスでは Tuple は使われない |
| A-6.2 使われる場合の書類型式 | N/A（不使用のため該当なし） |
| A-6.3 Context/Unit の継承 | N/A（不使用のため該当なし） |

### 情報源（Fact）

- [F1] K-2.a.md [F19] + K-2.12 — 仕様書全体検索で `ix:tuple` の記載が見つからない。「EDINET では ix:tuple は不使用と推定」
- [F2] パス: `docs/仕様書/提出者別タクソノミ作成ガイドライン.md` L.2077-2082 — 図表 5-2-9 substitutionGroup の設定値:

  | No | 定義した要素 | 値 |
  |---|---|---|
  | 1 | XBRL ディメンションの表を表す要素 | `xbrldt:hypercubeItem` |
  | 2 | XBRL ディメンションの軸を表す要素 | `xbrldt:dimensionItem` |
  | 3 | 目次項目 | `iod:identifierItem` |
  | 4 | No1〜No3 以外の要素 | `xbrli:item` |

  `xbrli:tuple` は一覧に含まれていない。
- [F3] `docs/仕様書/` 配下全ファイルの `tuple`（大文字小文字無視）全文検索 — 0 件ヒット
- [F4] サンプルインスタンス全 `.xbrl` ファイルでの `tuple` 検索 — 0 件ヒット（K-2.a.md の調査時に確認済み）

### 推論（Reasoning）

1. [F2] より、EDINET の提出者別タクソノミで定義可能な要素の `substitutionGroup` は `xbrldt:hypercubeItem`, `xbrldt:dimensionItem`, `iod:identifierItem`, `xbrli:item` の 4 種類のみ。`xbrli:tuple` は含まれていない。つまり、提出者がタクソノミ内に Tuple 型の要素を定義する手段がない。

2. [F3] より、EDINET の全仕様書において `tuple` という単語自体が一切出現しない。仕様として Tuple の使用を想定していないことが明白。

3. [F1] より、K-2.a.md の調査でも同様の結論（iXBRL の `ix:tuple` も不使用）が得られている。

4. [F2] の `substitutionGroup` 一覧から、EDINET は Tuple の代わりにディメンション（`xbrldt:hypercubeItem`, `xbrldt:dimensionItem`）を使用して Fact の構造化を行っている。これは XBRL 2.1 の Tuple に代わる現代的な構造化手法（XBRL Dimensions 仕様）に則っている。

5. [F1]+[F2]+[F3]+[F4] より、仕様書にも実データにも Tuple の痕跡がない。パーサーで Tuple をサポートする必要はない。

### 確信度

- 高（仕様書に `tuple` の記載なし + substitutionGroup に `xbrli:tuple` なし + 全サンプルで 0 件）
- XBRL 2.1 仕様では Tuple はサポートされているが、EDINET 固有の制約として不使用であることが確認済み

### 検証

- 検証者: Claude Opus 4.6
- 検証日: 2026-02-24
- 判定: **OK**

#### Step 1: 完全性検証 — OK
q.md の 3 つのサブ質問（A-6.1, A-6.2, A-6.3）すべてに回答がある。A-6.1 が NO であるため A-6.2, A-6.3 が N/A となるのは論理的に妥当。

#### Step 2: Fact 検証 — 全件一致
- [F2] `docs/仕様書/提出者別タクソノミ作成ガイドライン.md` L.2077-2082 を実ファイルで確認。図表 5-2-9 の内容は回答の引用と完全一致。4 行のみで `xbrli:tuple` は含まれていない。確認済み。
- [F3] `docs/仕様書/` 配下の全ファイルで `tuple`（大文字小文字無視）を grep 検索 → 0 件ヒット。回答の記載と一致。
- [F4] サンプルインスタンス全 `.xbrl` ファイル（56 ファイル）で `tuple`（大文字小文字無視）を grep 検索 → 0 件ヒット。回答の記載と一致。

#### Step 3: 推論検証 — 妥当
1. 推論 1: [F2] の substitutionGroup 一覧に `xbrli:tuple` が存在しない → 提出者タクソノミで Tuple 定義不可。論理的に正しい。
2. 推論 2: [F3] の全文検索 0 件 → 仕様として想定なし。妥当。
3. 推論 3: [F1] の K-2.a.md 調査との整合性。独立した裏取りとして有効。
4. 推論 4: ディメンションで構造化を代替しているという解釈は、substitutionGroup に `xbrldt:hypercubeItem`/`xbrldt:dimensionItem` が存在する事実から自然に導かれる。
5. 推論 5: 総合結論として「パーサーで Tuple 非サポートで可」は全 Fact から論理的に導かれる。

#### 指摘事項
なし。
