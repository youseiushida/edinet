name: Dump Parquet

on:
  workflow_dispatch:
    inputs:
      start:
        description: "開始日 (YYYY-MM-DD)"
        required: true
        type: string
      end:
        description: "終了日 (YYYY-MM-DD)"
        required: true
        type: string
      doc_types:
        description: "書類種別コード (スペース区切り, 空=全有報系)"
        required: false
        default: "120"
        type: string
      mode:
        description: "実行モード"
        required: false
        default: "process-pool"
        type: choice
        options:
          - async
          - thread-pool
          - process-pool
      max_workers:
        description: "Thread/ProcessPool ワーカー数"
        required: false
        default: "4"
        type: string
      concurrency:
        description: "同時ダウンロード数"
        required: false
        default: "16"
        type: string

jobs:
  dump:
    runs-on: ubuntu-latest
    timeout-minutes: 330  # 5.5h (6h 上限に余裕)
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync --extra analysis

      - name: Speed Test
        run: |
          pip install speedtest-cli
          speedtest-cli --simple

      - name: Install taxonomy
        run: |
          uv run python -c "
          import edinet
          edinet.configure(api_key='dummy')
          info = edinet.install_taxonomy(year=2025)
          print(f'TAXONOMY_PATH={info.path}')
          " | tee /tmp/taxonomy_env
          echo "TAXONOMY_PATH=$(grep TAXONOMY_PATH /tmp/taxonomy_env | cut -d= -f2)" >> "$GITHUB_ENV"

      - name: Build mode flags
        id: flags
        run: |
          MODE="${{ inputs.mode }}"
          if [ "$MODE" = "thread-pool" ]; then
            echo "flag=--thread-pool" >> "$GITHUB_OUTPUT"
          elif [ "$MODE" = "process-pool" ]; then
            echo "flag=--process-pool" >> "$GITHUB_OUTPUT"
          else
            echo "flag=" >> "$GITHUB_OUTPUT"
          fi

      - name: Run dump pipeline (${{ inputs.mode }})
        env:
          EDINET_API_KEY: ${{ secrets.EDINET_API_KEY }}
        run: |
          uv run python tools/dump_securities_reports.py \
            --start "${{ inputs.start }}" \
            --end "${{ inputs.end }}" \
            --doc-types ${{ inputs.doc_types }} \
            ${{ steps.flags.outputs.flag }} \
            --max-workers ${{ inputs.max_workers }} \
            --concurrency ${{ inputs.concurrency }} \
            --api-key "$EDINET_API_KEY" \
            --output-dir ./parquet_output

      - name: Upload Parquet artifacts
        uses: actions/upload-artifact@v4
        with:
          name: parquet-${{ inputs.mode }}-${{ inputs.start }}-${{ inputs.end }}
          path: ./parquet_output/
          retention-days: 1
          compression-level: 0  # Parquet は既に zstd 圧縮済み
